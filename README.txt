This is a sample app built on top of the base-app. It has been integrated with Spring. See the war/web-inf/lib/*.jar files (included in .classpath).

Building a MVP/DI/UiBinder/Spring sample app

A GwtRpcDispatcher has been included to forward request to the appropriate gwt servlets