This is a bare GWT application created with the GWT project creator. It has been integrated with Spring. See the war/web-inf/lib/*.jar files (included in .classpath).

A GwtRpcDispatcher has been included to forward request to the appropriate gwt servlets